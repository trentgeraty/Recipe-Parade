<div class="card">
    <div class="card-header">
        <h4>{{recipe.title}}</h4>
    </div>
    <p class="card-body">{{recipe.ingredients}}</p>
    <p class="card-body">{{recipe.directions}}</p>
    <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    
</div>